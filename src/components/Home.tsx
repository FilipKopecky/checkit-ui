import React from "react";
import {Box, Typography} from "@mui/material";

const Home: React.FC = () => {
  return (
    <Box>
        <Typography variant={"h1"}>
            CheckIt
        </Typography>
    </Box>
  );
};

export default Home;
